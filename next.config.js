/** @type {import('next').NextConfig} */
const nextConfig = {
    /*
    Since Next 14 Server Actions are not experimental anymore!
    experimental: {
        serverActions: true,
    },*/
}


module.exports = nextConfig
